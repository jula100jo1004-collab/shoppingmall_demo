{
  "name": "shopping-mall-demo",
  "private": true,
  "description": "Heroku deploys the Express API from /server",
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "start": "node server/src/server.js",
    "heroku-postbuild": "cd server && npm ci --omit=dev"
  }
}
